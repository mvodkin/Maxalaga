class MovingObject {
  constructor(options) {
    this.pos = option.pos;
    this.vel = options.vel;
    this.radius = options.radius;
    this.color = options.color;
  }

}

module export MovingObject;
